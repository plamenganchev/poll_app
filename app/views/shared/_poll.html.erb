<div class="col-md-2 col-md-offset-1 whitebackground">
<h1>IT WORKED</h1>
 <div class="field">
  <%= form_for :vote,url: votes_path,method: :post do |f| %>
  <h1><%= @poll.question %></h1>
   <%= f.hidden_field :poll_id, value: @poll.id  %>
   <%= f.hidden_field :p, value: @poll.id  %>
   <% @poll.answers.each do |l| %>
   <h2><%=l.answer_content %></h2>
   <%= f.radio_button :answer_id, l.id%>
   <%end%>
   <br/>
   <%= f.submit "Vote",class: "btn btn-primary" %>
  <%end%>
</div>
</div>